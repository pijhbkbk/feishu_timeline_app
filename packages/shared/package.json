{
  "name": "@feishu-timeline/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "pnpm exec eslint src --ext .ts --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('packages/shared: no tests yet')\""
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
