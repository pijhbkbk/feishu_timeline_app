# R24 Authentication, OAuth and Session Report

Final commit: `d86c04e8c016a0241172fb7c608f55d8dfcca5c9`

## OAuth and session controls

| Control | Result | Evidence |
|---|---|---|
| State generated server-side | PASS | Cryptographic UUID generated by API |
| State server-side storage and expiry | PASS | Redis-backed record, 600-second TTL |
| State bound to browser and one-time | PASS | HttpOnly state Cookie plus atomic consume; replay rejected |
| Fixed authorization endpoint | PASS | HTTPS, approved Feishu host and exact path; query/credentials/hash rejected |
| Fixed production callback | PASS | Must equal `FRONTEND_URL/login/callback` |
| Open redirect | PASS | No caller-controlled post-login redirect accepted |
| Token in URL/log/localStorage | PASS | No application code path; log keyword count 0 in host sample |
| Session Cookie | PASS | HttpOnly, SameSite=Lax, Secure in production, path `/`, explicit expiry |
| Expired session | PASS | Deleted server-side and rejected |
| Logout | PASS in current automated regression | Server-side session deletion and matching Cookie clear asserted |
| Empty/malformed callback | PASS after R24 fix | Controlled 401; no 500 and no code exchange |

The final empty-body callback probe returned `401 Unauthorized`; the earlier safe OpenAPI scan's callback 500 was fixed by `d86c04e8c016a0241172fb7c608f55d8dfcca5c9`.

## Browser boundary

| Control | Result |
|---|---|
| Same-origin unsafe request | Reaches authentication and returns 401 when unauthenticated |
| Cross-origin unsafe request | Rejected with 403 before business handling |
| CORS credentials origin | Fixed configured frontend origin; no wildcard |
| Missing Origin | Allowed for non-browser clients; session Cookie remains SameSite=Lax |
| Production/staging CSP | Nonce-based `script-src` with `strict-dynamic`; no script `unsafe-inline` or `unsafe-eval` |

Code-level OAuth/session status is `PASS`. The separate authenticated-ZAP evidence gate is `INCOMPLETE`; see `R24_DAST_REPORT.md`. The real account was deliberately not logged out at the end of R24 because exporting or destroying its only available test session was not necessary for the current-code automated logout proof.
