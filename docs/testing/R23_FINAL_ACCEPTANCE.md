# R23 Final Acceptance

## 1. 当前结论

R23：`BLOCKED / NOT PASSED`。

应用级 P0/P1 已清零，当前版本本地完整回归通过；但真实角色矩阵和 2 小时认证耐久是明确验收门禁，不能用 mock 角色或 10 秒冒烟替代。

## 2. 门禁状态

| 门禁 | 状态 |
|---|---|
| P0 = 0 | PASS |
| P1 = 0（未关闭） | PASS |
| 无重复流程节点 / 无丢失更新 | PASS（当前自动化范围） |
| 无数据库死锁 / 容器重启 | PASS（短时范围） |
| 无持续 loading | PASS |
| 主流程、退回、月度评审、退出、复盘 | PASS（当前版本 local 自动化） |
| 角色权限矩阵 | BLOCKED：真实飞书角色不足 |
| 2 小时耐久 | BLOCKED |
| 20 VU × 5m 只读 | PASS：5600 请求，0 错误/5xx，p95 46.17 ms |
| 全量测试 | PASS，50/50 Playwright、230 单元测试 |
| 所有应用证据同一最终 commit | PASS：`69d3332f30d6a7354c9b252d911cfe0a2652f76e` |

## 3. 已关闭 P1

1. 条件更新阻止并发陈旧动作复活已完成节点。
2. 中文附件名恢复与合法换版 multipart 边界修复。
3. 定时提醒在 Redis 入队边界原子去重。

## 4. 解除阻塞所需输入

1. 提供其余飞书测试成员，或允许在独立 staging 管理界面逐次切换现有测试成员角色。
2. 确认允许在 staging 提交创建七条 `R23-UAT-*` 记录。
3. 提供不暴露账号密码/Cookie 的认证负载身份方案，或由用户在受控环境注入 `R23_STAGING_SESSION_COOKIE` 后运行脚本。

解除后必须重新部署准确候选 commit，完成正式耐久矩阵、真实角色路径和最终全量回归，再更新为 PASS。当前禁止进入 R24、生产部署、`main` 合并或 tag。
