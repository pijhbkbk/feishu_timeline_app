# R23 Bug Tracker

## 1. 分级统计

| 等级 | 发现 | 已修复 | 未关闭 |
|---|---:|---:|---:|
| P0 | 0 | 0 | 0 |
| P1 | 4 | 4 | 0 |
| P2 | 0 | 0 | 0 |
| P3 | 0 | 0 | 0 |

## 2. 产品缺陷

| ID | 等级 | 复现 | 根因 | 修复 | 复测 |
|---|---|---|---|---|---|
| R23-P1-001 | P1 | 并发 `SUBMIT` 与陈旧 `START` 都返回 201，已完成节点被恢复为 `IN_PROGRESS` | 流转先读后无条件 `update`，缺少状态/活跃条件更新 | 所有关键流转改为按 `id + status + isActive` 条件更新，更新数不是 1 时返回 409 | R23-002、R23-005、R23-011、全量 50 条 Playwright PASS |
| R23-P1-002 | P1 | 中文附件名写入 mojibake；合法换版请求返回 `Too many parts` | multipart 文件名未恢复 UTF-8；Busboy 的 `parts=5` 边界无法容纳 1 文件 + 4 字段 | 上传拦截器安全恢复有效 UTF-8；`files=1`、`fields=4` 不变，严格 part 边界调整为 6 | 上传策略 7 条单测、R23-004、R23-009、全量回归 PASS |
| R23-P1-003 | P1 | 到期扫描连续执行两次，两次均入队 74 条 | 去重只查已落库通知，没有覆盖 Redis 队列待消费窗口 | 使用 Redis Lua 原子 `SET NX EX + LPUSH`；Redis 故障回退内存时也维护 TTL 去重表 | 队列 6 条单测、R23-014、全量回归 PASS |
| R23B-P1-004 | P1 | 真实飞书用户进入新建项目即提示“当前角色无权访问该功能”，人工测试无法开始 | 旧角色隔离策略仍把普通查看者限制为只读，与用户确认的当前使用策略不符 | 在统一认证用户出口赋予完整有效权限，保留未登录/停用/锁定边界和全部业务状态门禁 | API 权限 11/11、R20 Playwright 13/13、主链路 E2E PASS；隔离库权限用例 PASS |

## 3. 测试脚本缺陷（不计产品 P1）

- 采购角色最初被错误用于创建项目，后端正确返回 403；改为项目经理建项后切换采购。
- 陈旧动作原断言为 400；产品并发冲突语义统一为 409 后同步断言。
- R22 工作区 test id 和 `flow-map` 拦截路径曾使用旧值，已改为当前页面实现。
- 新隔离库发现两个 R22 用例依赖 seed 账号预先存在活跃任务，R23-014 又与自动调度存在竞争；按连续失败停止协议记录为回归夹具 blocker，未在本轮继续扩修。

所有失败均保留 Playwright screenshot/video/trace；修复后重新执行目标用例与完整回归。

## 4. 外部阻塞

| ID | 状态 | 描述 | 解除条件 |
|---|---|---|---|
| R23-BLOCK-001 | RESOLVED_BY_POLICY | 九类真实角色矩阵 | 用户已明确改为所有已认证用户全权限，不再要求九个真实 OAuth 用户 |
| R23-BLOCK-002 | OPEN | 不读取/导出登录 Cookie，因此尚无安全方式让负载脚本执行认证后业务查询/写入 | 提供一个受控的 staging 测试会话注入方式，或批准专用性能测试身份方案 |
| R23B-BLOCK-003 | OPEN | 全新隔离库完整 Playwright 有 3 个既有测试夹具/调度竞争失败 | 让 R22 用例自行创建活跃任务，并隔离 R23-014 的后台调度后重新执行全量回归 |
