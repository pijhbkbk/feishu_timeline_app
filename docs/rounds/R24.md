# R24 — Full Security Reaudit, Hardening and Remediation

## Goal and scope

Re-audit the current R23F code and staging artifacts, remediate discovered Critical/High issues, retest the complete application, collect private-cloud and Feishu admin evidence, and make a strict release-gate decision. R19 PASS results were not reused as current evidence.

Active scans were limited to local authorized staging. Production received only passive headers, health, TLS and read-only host/deployment checks. No production deploy, destructive scan, real-data deletion, main merge or tag occurred.

## Exact baseline and final candidate

- R23F evidence baseline: `c41553be92f7b6efddcea7104b15d9c991a7b9cc`.
- R23F application/staging baseline: `f0de3dd85fee0d69a2f33ac0f32f600b2826207c`.
- Initial R24 candidate: `d86c04e8c016a0241172fb7c608f55d8dfcca5c9`.
- Final R24B application/staging candidate: `f00703ac7834837f9ad573bc11d779a5caa7c02f`.
- Branch: `security/r24-full-reaudit`.

## Application and deployment changes

| Commit | Change |
|---|---|
| `1bd6a4e` | Harden five-image supply chain, same-Origin unsafe-method boundary, upload filename/double-extension handling, OAuth configuration and R24 IDOR tests |
| `299bf4a` | Suppress Nginx version disclosure |
| `fa0a4cd` | Force proxy recreation so runtime configuration matches the release |
| `d86c04e` | Return controlled 401 for empty OAuth callback bodies instead of 500 |

The initial infrastructure image scan contained 11 Critical and 119 High vulnerability occurrences. The final five exact images contain no reported vulnerability at any severity.

## Final checks

```text
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm --filter web build
pnpm --filter api build
pnpm --filter api prisma:validate
pnpm test:e2e
pnpm playwright:test
pnpm security:sast
pnpm security:secrets
pnpm 11 audit --all / --prod
OSV Scanner 2.4.0
Trivy filesystem and five exact images
Trivy CycloneDX SBOM generation
ZAP baseline
ZAP OpenAPI safe-mode scan
security header/CORS/Origin probes
RUN_SEED=no pnpm deploy:staging
production passive headers/health/TLS
GCE/SSH read-only host inspection
Feishu developer-console read-only inspection
```

Results:

- Web 25 files, 78/78 tests; API 54 files, 186/186 tests.
- Mainline E2E PASS; Playwright 52/52 PASS in 5.6 minutes.
- lint, typecheck, Web/API builds and Prisma validate PASS.
- SAST 0 findings; current/history secret scans 0 findings.
- pnpm 11/OSV/Trivy filesystem and final five-image SCA all severities 0.
- ZAP baseline C/H/M 0, Low 1, Info 8.
- OpenAPI safe import 165 URLs, WARN/FAIL 0 after callback remediation.
- Final staging: five services healthy, 18 migrations, no pending migration.
- Real final-session smoke: projects/admin loaded for 李晓晨 without exporting session material.

## Evidence findings

- Private cloud: key-only SSH, root login disabled, app services non-root, env `0600`, PostgreSQL/Redis loopback, TLS/HSTS/CSP present, backup checksum and restore evidence pass, remote tracked tree clean.
- Open Medium host finding: GCP default SSH ingress is global.
- Feishu evidence is now available, but configuration fails minimum-permission/release review: formal localhost callback, unused Contact scopes, empty mobile homepage and H5 trusted domain.
- Authenticated ZAP passive and approved authenticated low-risk active scans were not completed because the only real session was not exported.

## Severity and decision

Final consolidated open findings: Critical 0, High 0, Medium 3, Low 1, Info 11. None of the three Medium findings has formal risk acceptance.

`R24_FAIL / AUTHENTICATED_DAST_INCOMPLETE / FEISHU_CONFIGURATION_FAIL / OPEN_HOST_MEDIUM / NO_JOINT_RELEASE_GATE / STOP` (superseded by the R24B closure below)

Reports are under `docs/security/R24_*.md`. R24 stops here and does not deploy production.

## R24B security-gate closure — 2026-07-17

R24B was limited to the failed configuration/evidence gates and their affected regression. It did not repeat the already-passed large SAST/SCA program, deploy application code to production, scan production or Feishu domains, merge main, create a tag, or enter R25.

### Closure changes

- `83e5c752fd3cc66faa56c5e9db56933490c1b7c6` reconciles the same real person's app-scoped Feishu identities across the formal and test applications, with six regression tests.
- `f00703ac7834837f9ad573bc11d779a5caa7c02f` applies CSP to `/favicon.ico` and adds middleware matcher regression coverage after authenticated ZAP identified plugin `10038` on that path.
- Staging was rebuilt at the exact final commit before the final full authenticated scan and regression.

### Configuration and DAST results

- `R24-HOST-001` Fixed: IAP API enabled, `iap-ssh` tag added, `allow-iap-ssh` restricted to `35.235.240.0/20`, two pre-disable and one post-disable IAP SSH checks passed, and `default-allow-ssh` is disabled but retained for rollback.
- `R24-FEISHU-001` Fixed: the formal application retains only the exact production HTTPS callback; real production OAuth/session/logout passed after credential rotation.
- `R24-FEISHU-002` Fixed: all unused Contact/API permissions were removed and published; Contact data range is N/A.
- `R24-FEISHU-003` Fixed: partial-member availability, desktop/mobile homepages and H5 trusted domain were confirmed.
- `R24-CRED-001` Fixed: the formal credential was rotated without retaining its value; production health and real OAuth/session/logout/post-logout denial passed.
- `R24-EVID-001` Fixed: authenticated ZAP reached six protected endpoints, proved the unauthenticated project endpoint returns 401, recorded non-zero authentication statistics, and completed strict-scope Spider, AJAX Spider, GET-only OpenAPI and low-strength active scanning.
- Final authenticated ZAP: Critical 0, High 0, Medium 0, Low 3, Informational 4. The Low/Info results are documented triage; the two TLS-header Low alerts belong to the ephemeral scan alias proxy, not a deployed endpoint.
- Cleanup: `authMaterialUsed=true`, `authMaterialDestroyed=true`, `serverSessionInvalidated=true`; raw reports are mode 600 and repository-external session material is absent.

### Final regression

```text
pnpm lint                                      PASS
pnpm typecheck                                 PASS
pnpm test                                      Web 79/79; API 187/187
pnpm --filter @feishu-timeline/web build       PASS
pnpm --filter @feishu-timeline/api build       PASS
pnpm --filter @feishu-timeline/api prisma:validate PASS
pnpm test:e2e                                  PASS
pnpm playwright:test                           52/52 PASS (5.2m)
pnpm security:secrets                          current/history PASS, 0 findings
```

The Playwright run includes the complete R23 stability suite and the project/workflow/review/upload/audit/retrospective paths. The only real account was used for real OAuth and the authorized administrator positive path; deterministic automated identities supplied negative role and IDOR coverage. No claim of nine real accounts is made.

### Final decision

`R24B_PASS / R24_PASS / R25_JOINT_RELEASE_GATE_ALLOWED_BUT_NOT_STARTED / STOP`

R24 is now passed. Work stops before R25 pending the user's explicit confirmation.
