# R23B — 真实角色矩阵、真实写路径与认证耐久收口

## 目标

仅闭合 R23 的四项剩余证据：九个真实业务角色用户、七条 staging UAT 写路径、认证 5 VU × 2h、认证 10 VU × 30m。

## 基线

| 字段 | 值 |
|---|---|
| applicationCommit | `69d3332f30d6a7354c9b252d911cfe0a2652f76e` |
| evidenceCommit（输入） | `1684b67a7dc6bbd340c757033cc7e25c4bcda692` |
| stagingCommit | `69d3332f30d6a7354c9b252d911cfe0a2652f76e` |
| 分支 | `release/r22-stability-security-rc` |
| migration | `16 completed / 0 pending` |
| staging | 五项服务 healthy，mock 关闭，真实飞书入口可用 |
| 阶段 0 核验时间 | `2026-07-14 18:14 CST` |

`1684b67` 相对 applicationCommit 只修改 R23 报告，不包含应用代码变更。

## 业务身份到系统角色映射

| 业务身份 | 系统角色 |
|---|---|
| marketing | `project_manager` |
| coating_process | `process_engineer` |
| procurement | `purchaser` |
| quality | `quality_engineer` |
| production | `process_engineer` |
| finance | `finance` |
| project_manager | `project_manager` |
| admin | `admin` |
| viewer | `viewer` |

现有角色模型只有八种系统角色；marketing/project_manager 与 coating_process/production 分别共享系统角色。R23B 仍要求九个不同的真实飞书 OAuth 用户，不以一个管理员切换角色，也不以 `mock_*` 演示用户替代。

## 阶段 0 结果

- staging 用户记录共 9 条，但只有 1 条是真实飞书用户；7 条为 `mock_*`，1 条为本地系统管理员。
- 唯一真实飞书用户映射 `admin + viewer`。
- 当前 `R23-UAT-*` 项目数为 0。
- 浏览器确认真实飞书登录入口可用、模拟登录关闭；未读取或导出会话数据。
- `test-results/r23/.auth/*` 已被仓库现有 `test-results/` 规则忽略。
- 本轮没有创建项目、修改角色、执行写接口或运行认证负载。
- 提交前工程检查通过：Web 73/73、API 157/157，lint、typecheck、双端 build、Prisma validate 均通过。

## 阻塞与解除条件

1. 在飞书预发布应用可用范围内准备九个不同测试用户，并分别映射上述业务身份。
2. 用户明确授权仅在独立 staging 创建和推进七条 `R23-UAT-*` 项目，测试结束只归档、不硬删除。
3. 九个用户分别完成人工 OAuth；会话仅保存于 `test-results/r23/.auth/`，权限 600，不提交、不打印、不上传。
4. 条件齐备后从阶段 1 继续真实角色矩阵、七写路径和两档认证耐久，不重跑已通过的 R23 主体。

## 当前决策

`BLOCKED / STOP`。

不得进入 R24、部署生产、合并 `main` 或创建 tag。
