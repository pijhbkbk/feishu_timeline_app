# R23 — 真实使用稳定性、UAT 与 Bug 修复

## 目标

在 R22 生产完成后的当前准确版本上，验证真实业务路径、并发一致性、异常恢复和耐久性，并关闭所有 P0/P1。

## 范围

- 七类 `R23-UAT-*` 项目。
- 已认证用户全权限与未登录访问边界（2026-07-14 经用户确认替代九角色隔离矩阵）。
- 18 步主链路、评审、附件、收费、月度任务、退出、复盘、审计。
- 并发、重复、陈旧页面、网络中断、会话失效、500/401、定时任务。
- staging 5/10/20 VU 温和负载。

## 当前结果

- 候选分支已创建并推送。
- 独立 staging 已部署，mock 关闭，真实飞书 OAuth 登录成功。
- R23 专项 14/14、核心业务重跑 13/13、完整 Playwright 50/50、Web 73/73、API 157/157 通过。
- 累计修复 4 个 P1：并发节点复活、附件中文名/换版边界、提醒重复入队，以及人工测试被角色限制阻断。
- 应用 commit `69d3332f30d6a7354c9b252d911cfe0a2652f76e` 已准确部署；16 个 migration 无待执行项，五项服务和 HTTP 检查 healthy。
- 20 VU × 5m 只读档 5600 请求、0 错误/5xx、p95 46.17 ms；空闲回收后 API+Web 合计内存较基线增长 4.87%。
- 2026-07-14 最新候选的定向权限门禁通过；完整 Playwright 在全新隔离库为 43 PASS / 3 FAIL / 4 NOT RUN，失败来自既有活跃任务夹具与提醒调度竞争。
- 当前门禁：七条 staging 真实写路径与认证后 2 小时耐久未执行；最新全量回归另被既有测试夹具阻塞。

## 决策

状态：`BLOCKED`。不得自动进入 R24。

解除条件和证据路径见：

- `docs/testing/R23_TEST_RUN_REPORT.md`
- `docs/testing/R23_BUG_TRACKER.md`
- `docs/testing/R23_PERFORMANCE_REPORT.md`
- `docs/testing/R23_FINAL_ACCEPTANCE.md`
