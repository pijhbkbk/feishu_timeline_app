# R23 — 真实使用稳定性、UAT 与 Bug 修复

## 目标与策略

在独立 staging 验证真实业务路径、并发一致性、异常恢复和耐久性，并关闭所有 P0/P1。用户于 R23B 确认取消角色隔离，产品策略为所有有效已认证用户完整权限；九角色矩阵不再适用，未登录边界仍必须拒绝。

## 最终应用与 staging

- 分支：`release/r22-stability-security-rc`
- applicationCommit：`d6d4962f88dbb5b297d54c9f27326f3bf5616ec7`
- stagingCommit：`d6d4962f88dbb5b297d54c9f27326f3bf5616ec7`
- evidenceCommit：`PENDING_EVIDENCE_COMMIT`
- API/Web image：`r23d-d6d4962f88db`，OCI revision 与完整 SHA 一致
- migration：17，pending：0
- PostgreSQL、Redis、API、Web、Nginx：healthy

## R23B 已完成

- 使用一个真实飞书 OAuth 账号完成七条权威 `R23-UAT-*` 写路径：正常主线、评审退回、非阻塞、逾期、材料版本、月度跟踪、并发编辑全部 PASS。
- 同节点双标签同时写入一成一拒，用户看到中文冲突；自动化确认 409、幂等和完成节点不复活。
- 中文材料名、V1→V2、同名不覆盖、下载、匿名 401、上传中断无半记录均通过。
- 12 个月实例不重复；首月 1/12 后第 17 步保持活跃、第 18 步未触发；逾期投影正确。
- 累计 P0 0、P1 6/6 修复、P2 2/2 修复、P3 0。
- 最终回归：Web 74/74、API 163/163、主链路 E2E PASS、Playwright 52/52；lint、typecheck、build、Prisma validate 全部 PASS。
- 七条权威项目和一条已替代项目均已写入“已归档 / 测试项目”逻辑标记，审计保留，无物理删除。

## 未完成与决策

- 20 VU × 5m 未认证只读参考：5600 请求，0 error/5xx，p50/p95/p99 `7.04/46.17/74.91 ms`，空闲回收后内存增长 4.87%。
- 认证 `5 VU × 2h`、`10 VU × 30m` 未执行：禁止读取或导出真实会话机密，当前没有安全会话注入方式（`R23-BLOCK-002`）。

状态：`BLOCKED / NOT PASSED`。不得进入 R24、部署生产、合并 main 或创建 tag。

证据路径：

- `docs/testing/R23_TEST_RUN_REPORT.md`
- `docs/testing/R23_BUG_TRACKER.md`
- `docs/testing/R23_PERFORMANCE_REPORT.md`
- `docs/testing/R23_FINAL_ACCEPTANCE.md`
- `test-results/r23/r23-run-manifest.json`（Git ignored）

## R23C 认证耐久继续执行（2026-07-16）

- 真实飞书 OAuth 受控会话注入已解除 `R23-BLOCK-002`；认证材料在测试停止时完成 201 logout 并销毁。
- `5 VU × 2 h` PASS：32,539 请求、0 error/auth/5xx、read p95 736.946 ms、write p95 133.294 ms、回收后内存增长 -59.6311%、deadlock/restart 0。
- 首次 `10 VU × 30 m` FAIL：14,767 请求中 7 次项目日志读取超时，read p95 1104.928 ms；0 auth/5xx。
- 根因是日志 API 忽略分页并返回23,179条审计记录（约11.1 MB）。`a4a9efd50404a512102dd74d1ab18d9bceb971a9` 已实施有界跨来源分页、当页详情查询、前端加载更多和请求 ID 证据；API 163/163、Web 74/74及工程门禁通过。
- staging 部署连续两次因外部 registry 失败，候选未切换；active staging 仍为 `cdb51963502e35004bf2667aec7c8b7a49a51e25`，五服务 healthy、restart 0。

状态：`R23C_BLOCKED / R23_NOT_PASSED / STOP`。恢复后只部署 `a4a9efd`、重跑修复后 10 VU 与完整回归；不得进入 R24。详见 `docs/testing/R23C_BLOCKER_REPORT.md`。

## R23D staging artifact deployment and endurance closure（2026-07-16）

- 最终 application/staging commit：`d6d4962f88dbb5b297d54c9f27326f3bf5616ec7`。
- 本地缓存、无网络 overlay 镜像精确部署；17 migrations、0 pending、五服务 healthy、restart 0；API/Web 镜像扫描 PASS。
- 审计专项 PASS：23,189 条完整遍历且无重复/丢失，最大响应 48,714 bytes，p95 25.464 ms，筛选与独立详情接口通过。
- 10 VU × 30m PASS：17,997 请求、0 error/5xx/auth、HTTP p95 96.776 ms、恢复内存 -1.3618%。
- 5 VU × 2h PASS：29,658 请求、0 error/5xx/auth、HTTP p95 80.758 ms、恢复内存 -0.4664%。
- 两档 DB slow/deadlock、Redis queue、restart、日志异常与完整性异常均为 0；产品 P0/P1 为 0。
- lint、typecheck、Web 74/74、Web/API build、Prisma validate PASS；API socket transport、E2E、Playwright、Gitleaks 和 server logout 被后续受限沙箱的 localhost/Docker `EPERM` 阻断。
- 本地认证材料已销毁；不宣称 server logout 成功。

状态：`R23D_BLOCKED / R23_NOT_PASSED / STOP_BEFORE_R24`。恢复后只补跑受阻回归/cleanup，不重跑耐久。方案 A 最小权限仍为 R24 前置，尚未实施，避免改变已完成耐久所对应的 final application commit。

## R23E unrestricted final regression and evidence closure（2026-07-16）

- 权限预检：Git write、Docker、localhost listen 全部 PASS；application/staging revision 仍为 `d6d4962f...`。
- 应用冻结：R23E 相对 application commit 只有 docs 证据变更；无 apps/packages/prisma/deploy/runtime config 变更。
- API：51 files、166/166、0 failed、0 skipped；此前被 EPERM 阻断的 4 项全部实际执行。
- 主链路 E2E PASS；完整 Playwright 52/52 PASS、0 skipped、5.1m；Chrome for Testing 148.0.7778.96。
- lint、typecheck、Web 74/74、API 166/166、Web/API build、Prisma validate 全部 PASS。
- 真实飞书 OAuth logout：HTTP 201；旧 Session 返回未认证；对应 Redis Session record 为 0；临时认证目录为 0。
- Gitleaks 8.30.1：current tree 与 full history 均 PASS，0 findings。
- P0/P1/P2/P3 未关闭项 `0/0/0/0`；`R23D-BLOCK-005` RESOLVED。
- 同 commit 的审计、10 VU × 30m、5 VU × 2h 证据继续有效，不重跑。

状态：`R23E_PASS / R23_PASSED / STOP_BEFORE_R24`。本轮未部署生产、未合并 main、未创建 tag、未进入 R24。当前“所有有效已认证用户完整权限”仍是 R23 已验收策略；方案 A 最小权限是进入 R24 前必须实施和重新验证的下一项应用变更。
