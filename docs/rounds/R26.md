# R26 — 登录入口直达飞书

## 目标与边界

- 删除“系统页面 → 登录选择页 → 飞书”的多余交互层级。
- 顶部“登录”和未登录状态按钮一次点击直接请求后端飞书授权地址并跳转。
- 保留 `/login` 作为旧链接和直接访问的兼容入口，但页面只负责自动跳转，不再展示飞书/模拟登录选择表单。
- 不修改后端认证、权限、流程、数据库模型或 migration；不部署 staging/production，不改动 `v1.1.0-rc.1` 标签。

## 输入

- 用户提供的 2026-07-22 现网截图：主页面登录入口、登录选择页、飞书授权页。
- 基线：`release/r25-final-gate`，HEAD `1ea034c`，工作树初始 clean。

## 实现

- `apps/web/src/components/app-shell.tsx`
  - 顶部“登录”和“前往登录”在飞书可用时直接调用 `startFeishuLogin`。
  - 保留 `href="/login"` 作为渐进增强回退；直接请求失败时进入自动跳转入口重试并展示受控错误。
- `apps/web/src/app/login/page.tsx`
  - 删除登录选择、飞书二次按钮和模拟登录表单。
  - 未登录且飞书已配置时只触发一次自动 OAuth 跳转。
  - 已登录用户手工访问时返回工作台；飞书未配置或请求失败时展示明确错误。
- `apps/web/tests/playwright/regression.spec.ts`
  - 覆盖顶部登录直达飞书和旧 `/login` 自动直达两条路径。
- `apps/web/playwright.config.mjs`
  - 允许本机通过显式 `PLAYWRIGHT_EXECUTABLE_PATH` 使用已有浏览器；默认 CI 行为不变。

后端 mock-login 接口仍保留给本地自动化，但不再暴露在用户登录页面。

## 验收

```text
pnpm install                                      PASS
pnpm lint                                         PASS
pnpm typecheck                                    PASS
pnpm test                                         PASS; Web 83/83, API 223/223
pnpm --filter web build                           PASS
pnpm --filter api build                           PASS
pnpm --filter api prisma:validate                 PASS
target Playwright                                 PASS; 1/1, 14.6 s
```

定向 Playwright 同一用例验证：顶部登录未进入选择页即到达飞书目标；直接访问 `/login` 也自动到达飞书目标。首次执行在产品代码前被新版 Playwright 缺少 Chromium/FFmpeg 阻断；改用已安装 Chrome、补齐 FFmpeg 后，测试桩曾因 HTML 未声明 UTF-8 导致中文断言失败，修正测试桩字符集后原样重跑通过。以上均未改变产品验收断言。

## 风险与决定

- 本轮没有真实生产认证材料，没有访问或修改飞书后台，也没有部署 `timeline.all-too-well.com`；现网行为要在后续部署后才会改变。
- 飞书最终展示二维码还是已有账号的授权确认页，由飞书依据浏览器登录态决定；系统保证的是一次点击直达飞书认证页面。
- `v1.1.0-rc.1` 不包含本轮变更。若要发布本轮代码，需重新走相应候选构建、部署和发布门禁。

决定：`R26_LOGIN_DIRECT_FEISHU_LOCAL_PASS / NO_DEPLOY / NO_TAG_CHANGE / READY_FOR_NEXT_INTERACTION_CHANGE`。
