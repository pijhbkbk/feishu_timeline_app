# R25 — Combined Stability, Security and Release Gate

## Objective and boundaries

Unify stability, security, artifact, deployment, recovery and real staging UAT evidence on one final runtime, then stop before production for explicit approval. No production deployment/write/active scan, `main` merge, stable tag, credential disclosure or workflow-rule change is permitted. R25 is not a feature-development round.

## Runtime evolution

The original frozen runtime was `f00703ac7834837f9ad573bc11d779a5caa7c02f`. During R25, the progress UI attempted to submit more than the API attachment limit after long endurance history accumulated. The authorized self-repair limited submitted attachment IDs to the newest 20 while preserving history, and added cleanup hardening:

- `437c0d881efa...` — progress evidence/load cleanup fix and regression coverage.
- `6d24378168fd144e539b0e99f975b918b06e37a5` — complete immutable OCI candidate provenance.

All formal resumed load and UAT evidence uses final runtime/staging commit `6d24378168fd144e539b0e99f975b918b06e37a5`.

## Completed gates

| Gate | Result |
|---|---|
| release branch/baseline | PASS; `release/r25-final-gate` |
| R23 evidence review | PASS; R23 formally passed, historical endurance not reused |
| exact immutable build/deploy | PASS; `r25-6d2437818502`, complete OCI labels, `RUN_SEED=no`, 18 migrations/0 pending, five healthy/restart 0 |
| GCP IAP read-only recheck | PASS; IAP API/tag/rule correct, global default SSH disabled and retained |
| production anti-tamper | PASS; `7dd2243270c03399cd6da6cec41bf12eab68dd0b`, tracked changes 0 |
| fresh real Feishu OAuth | PASS; one real account, no multi-account claim |
| 10 VU × 30 m | PASS; 17,424/17,424 checks, read/write p95 106.338/58.429 ms, auth/5xx/functional 0, idle memory -8.255% |
| 5 VU × 2 h | PASS; 34,832/34,832 checks, read/write p95 119.742/44.276 ms, auth/5xx/functional 0, idle memory -0.602% |
| employee UAT | PASS; task 1.102 s, progress/material 58.770 s, first operation complete and second generated |
| project-manager UAT | PASS; risk 1.405 s, stalled operation in 2 clicks |

The earlier cold-baseline two-hour run's high-water RSS behavior is retained as a non-release observation. The formal warmed candidate run met the explicit idle-memory threshold and every functional/resource/integrity threshold.

## Repeated administrator-gate failure

Final administrator UAT requires:

```text
后台管理 → 审计日志 → 查询关键操作
```

and the regression gate requires `/admin/audit-logs`, bounded paging, independent detail reads, stable filtering/sorting and non-admin denial.

Attempt 1 used the fresh real OAuth Chrome session and found no usable audit query/list/detail interface. Attempt 2 used the same authenticated identity for read-only verification:

```text
/api/admin/overview                              200
/api/admin/audit-logs?page=1&pageSize=20       404
/admin/audit-logs                               200 placeholder
bounded list markup                             absent
```

Repository inspection confirms the route renders `PagePlaceholder` and the admin controller only exposes overview. This is `R25-ADMIN-001`, product severity P1. Project-scoped activity logs do not satisfy the global administrator gate.

Per `AGENTS.md`, the same gate failed twice. Because R25 forbids new business functionality, the round stops instead of implementing the missing surface inside the release gate.

## Sequential stop and cleanup

- Exact-final full quality, remaining R25 security closure, final image scan/SBOM, backup/restore, rollback/forward, management UAT, candidate evidence commit and candidate tag are NOT RUN/NOT CREATED.
- Repository-external load authentication was logged out successfully and destroyed.
- No Cookie, OAuth code, token, App Secret, storage state or database password was output or committed.
- Production was not changed; no `main` merge or tag occurred.

## Decision

`R25_BLOCKED / P0-P1-P2-P3=0-1-0-0 / PRODUCTION_NOT_AUTHORIZED / NO_CANDIDATE_TAG / STOP`

Safe resumption requires a separate authorized runtime-fix round for `R25-ADMIN-001`, followed by exact rebuild/deploy and repetition of both load profiles plus every affected downstream gate. See `docs/release/R25_BLOCKER_REPORT.md`.
