# R25 — Combined Stability, Security and Release Gate

## Objective

Unify stability, business regression, security, immutable artifact provenance, staging deployment, backup/restore, rollback and final user acceptance evidence on runtime commit `f00703ac7834837f9ad573bc11d779a5caa7c02f`, then stop before production for explicit approval.

## Frozen boundaries

- No business feature or workflow-rule change.
- No production deployment, production database write, production active scan or load test.
- No `main` merge or stable tag.
- No credential, cookie, OAuth code, token, storage state or database password in output or Git.
- Application images must retain `f00703a` as their OCI revision even though the release candidate evidence commit is newer.

## Execution status

| Stage | Result |
|---|---|
| 0 — branch and baseline | PASS; `release/r25-final-gate` pushed from R24B evidence commit |
| 1 — R23 evidence review | PASS; R23 formally passed but its pre-R24 endurance cannot be reused as final R25 evidence |
| 2 — immutable build and provenance | PASS; exact API/Web/PostgreSQL candidate images built and recorded; API/Web Trivy and SBOM pass |
| 3 — final staging deployment | PASS; exact runtime, `RUN_SEED=no`, 18 migrations/0 pending, five healthy services |
| 4 — real OAuth and business/security regression | real OAuth/project create PASS; complete regression pending |
| 5 — 10 VU × 30 m | PASS; 17,484 checks, 0 errors/auth/5xx, read/write p95 72.986/62.546 ms, zero restart/deadlock, idle memory -2.061% |
| 6 — 5 VU × 2 h | BLOCKED; first valid run failed idle memory `+60.2254%`; second run was interrupted at about 1 h 20 m, had no final summary and resumed after session expiry |
| 7 — complete quality regression | NOT RUN; sequential stop at stage 6 |
| 8 — R24 key security regression | PARTIAL; IAP/anti-tamper/API-Web image scans pass; remaining checks NOT RUN |
| 9 — staging backup/restore | NOT RUN; sequential stop at stage 6 |
| 10 — staging rollback/forward recovery | prerequisites PASS; execution NOT RUN |
| 11 — final staging UAT | NOT RUN; sequential stop at stage 6 |
| 12 — release documents and candidate tag | blocker documents complete; candidate tag NOT CREATED |

## Current decision

`R25_BLOCKED / PRODUCTION_NOT_AUTHORIZED / NO_CANDIDATE_TAG / NO_MAIN_MERGE / NO_STABLE_TAG / STOP`

Two consecutive stage-6 executions did not produce a passing 5 VU × 2 h result. The first completed run failed the explicit idle-memory threshold; the second was invalidated by execution interruption and session expiry. The orphan load container and repository-external authentication directory were destroyed, old protected requests were rejected, and final Gitleaks current/history scans passed with 0 findings. See `docs/release/R25_BLOCKER_REPORT.md`.
